# Hack-The-Ridge-Hackathon

The submission directory contains the main source files, to run:
    1. install all dependancies
    2. run Map.py with streamlit (streamlit run .\Map.py)
    3. run GUI.py with python (python GUI.py)